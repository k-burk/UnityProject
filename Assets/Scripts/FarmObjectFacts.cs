using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FarmObjectFacts : MonoBehaviour
{
    //when object is clicked 
    //change camera to which (camera zoomed in on object)
    //display text box with fact about object 
    //pla audio that reads whatever is in the text box 
    // Start is called before the first frame update
    /*var cam1 : Camera; //main camera
    var cam2 : Camera; //corn camera
    var cam3 : Camera; //carrot 
    var cam4 : Camera; //pumpkin
    var cam5 : Camera; //eggplant
    var cam6 : Camera; //tomato */


    void Start()
    {
       /* //when scene starts the main camera is enabled
        cam1.enabled = true; //enable player camera
        //cameras for fruit are dsiabled
        cam2.enabled = false; 
        cam1.enabled = false;
        cam2.enabled = false;
        cam1.enabled = false;
        cam2.enabled = false; */
    }

    // Update is called once per frame
    void Update()
    {
        /*if (Input.GetKeyDown(KeyCode.C))
        {
            cam1.enabled = !cam1.enabled;
            cam2.enabled = !cam2.enabled;
        }*/
    }
}
